name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.1.1'
      SERVERPOD_PASSWORD_database: 'YiWxV5JBREs6W_ORz46Kp3oCW0mMTw1e'
      SERVERPOD_PASSWORD_redis: '6gFvMKTb4Bb1V8MfWkvNqDs4GM5zxh8A'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: cognitive_load_butler_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: cognitive_load_butler_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: cognitive_load_butler_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: cognitive_load_butler_server
        run: serverpod generate

      - name: Run tests
        working-directory: cognitive_load_butler_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: cognitive_load_butler_server
        run: docker compose down -v
